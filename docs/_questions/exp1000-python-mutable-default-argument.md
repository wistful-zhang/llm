---
title: 'Python 可变默认参数为什么会跨调用共享？'
source: '公开岗位面试主题整理；答案为面试口述稿，待逐题资料复核'
verified: false
review_status: '待复习'
category: '编程与框架'
difficulty: '简单'
tags:
  - 'Python'
  - '函数'
published: true
answer_status: complete
date: 2026-07-22
---

## 面试时怎么答

组织答案时围绕“可变默认参数”展开：把定义或机制讲清楚，用具体例子验证，并说明类属性、闭包和缓存也可能产生类似共享状态，需要明确生命周期。

**可以这样答：**

> 可以从这条主线理解：默认参数在函数定义时创建一次，后续调用复用同一对象，因此列表或字典修改会泄漏到下一次。再用一个最小验证说明：实现错误的历史消息累积函数，再用 None 作为哨兵修复。最后明确限制：类属性、闭包和缓存也可能产生类似共享状态，需要明确生命周期。

## 常见追问

1. **不铺背景，直接说明“可变默认参数”的核心机制或判断。** 默认参数在函数定义时创建一次，后续调用复用同一对象，因此列表或字典修改会泄漏到下一次
2. **把“可变默认参数”落到一个可检查的例子，你会怎么做？** 实现错误的历史消息累积函数，再用 None 作为哨兵修复
3. **什么情况下不能直接套用“可变默认参数”？** 类属性、闭包和缓存也可能产生类似共享状态，需要明确生命周期

## 延伸阅读

- [Python 3 官方文档](https://docs.python.org/3/)
